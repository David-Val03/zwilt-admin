export interface IThemeBreakpoints {
    xs: number;
    sm: number;
    md: number;
    lg: number;
    xl: number;
}

const breakpoints: IThemeBreakpoints = {
    xs: 0,
    sm: 600,
    md: 960,
    lg: 1280,
    xl: 1920,
};

export default breakpoints;
